name: cardgame
description: "A Flutter card game by Everton."

publish_to: "none"

version: 1.0.0+2

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^6.1.0
  provider: ^6.0.1
  flutter_mobx: ^2.0.2
  hive: ^2.0.4
  hive_flutter: ^1.1.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.5
  mobx_codegen: ^2.3.0

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - images/
